# สุ่มรอบเมืองหลวงและแถบเวลามือถือ

Google Mode ในหมวดทั่วโลก/เมืองสุ่มพิกัดใหม่รอบเมืองหลวงจาก `data/capitals.ts` (51 แห่ง) ส่วนประเทศไทยสุ่มรอบกรุงเทพฯ หมวดธรรมชาติและแลนด์มาร์กยังใช้ชุดจุดเฉพาะเดิม เพื่อรักษาความหมายของตัวกรอง

ความยากกำหนดรัศมี: ง่าย 5 กม., ปกติ 12 กม., ยาก 25 กม. เป็นรัศมีรอบศูนย์กลาง ไม่ใช่ขอบเขตเทศบาลหรือประเทศ รายการนี้เป็นชุดเริ่มต้น ไม่ใช่เมืองหลวงครบทุกประเทศ และไม่ได้รับประกัน coverage ทุกจุด

สุ่มแบบกระจายตามพื้นที่บนทรงกลม ตรวจ Google Metadata สดก่อนใช้ทุกครั้ง ตรวจระยะจากจุดสุ่มและศูนย์กลาง ใช้พิกัด panorama จริงคำนวณคะแนน กัน pano ID ซ้ำในเกม/ประวัติล่าสุด และระยะห่างในเกมตาม `GOOGLE_MIN_DISTANCE_KM` จำนวนครั้งจำกัดด้วย `GOOGLE_MAX_ATTEMPTS` (1–12; ค่าเริ่มต้น 8) ใช้ API keys เดิม ไม่ต้องเพิ่ม environment variables

เมื่อไม่มี coverage ภายในจำนวนครั้งที่กำหนดใช้ fallback เดิม; เมื่อพบแต่ภาพซ้ำแจ้งให้ลองใหม่/ยืนยันล้างประวัติตามระบบเดิม ไม่รับประกันภาพไม่ซ้ำตลอดไป ไม่เก็บภาพหรือ Metadata ลง cache ถาวร เก็บประวัติ pano ID ตามระบบเดิมเท่านั้น ข้อจำกัดการเก็บข้อมูล: https://developers.google.com/maps/documentation/streetview/policies

มือถือ/แท็บเล็ตความกว้างไม่เกิน 850px มีแถบเวลาติดขอบบน พร้อมตัวเลขและแถบลดตามเวลาจาก server เปลี่ยนเป็นแดงเมื่อเหลือ 10 วินาที รองรับ safe area และ reduced motion ใช้ได้ทั้ง Demo และ Google รวมถึงขณะเลื่อนลงไปปักหมุด

ตรวจสอบ: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`
